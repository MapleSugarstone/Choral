# Chronicles kept for working on the replay view. They are exports of ordinary
# games, so they mean nothing to anyone else.
test replays/

# Windows writes these into any folder it has drawn thumbnails for, and Assets is
# the obvious candidate.
Thumbs.db
desktop.ini
.DS_Store

# League exports for local testing. Regenerated by `league export`; the shipped
# brain lives in ivy_pro.js instead, written only by `league export --pro`.
nets/
